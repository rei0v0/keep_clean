import 'package:flutter/material.dart';

class CleaningListPage extends StatelessWidget {

  const CleaningListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("first page"),
    );
  }

}